# Copie este arquivo para .env e preencha com os valores reais
# NUNCA commite o arquivo .env

# Banco de dados
DB_USER=
DB_PASSWORD=

# JWT - gere com: [Convert]::ToBase64String((1..32 | ForEach-Object { [byte](Get-Random -Max 256) }))
JWT_SECRET=